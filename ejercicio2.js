// 2.	Escribe una función que reciba un array de números ordenados de forma ascendente y devuelva el o los números faltantes en la secuencia.

let arr = [1,3,5]
function numFaltantes(arr) {
    let newArr = [] ;
    for (let index = 0; index < arr.length; index++) {
        
    }
}
